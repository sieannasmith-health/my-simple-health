(()=>{const reduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const reveal=[...document.querySelectorAll('.msh-reveal')];const kinetic=document.querySelector('[data-kinetic]');if(reduced){reveal.forEach(el=>el.classList.add('is-visible'));if(kinetic)kinetic.classList.add('is-visible');return;}const io=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');io.unobserve(entry.target);}})},{threshold:.16,rootMargin:'0px 0px -6% 0px'});reveal.forEach(el=>io.observe(el));if(kinetic){const kineticObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('is-visible');else if(entry.boundingClientRect.top>0)entry.target.classList.remove('is-visible');});},{threshold:.34});kineticObserver.observe(kinetic);}})();