/* My Simple Health — Progress: movement without grading */
(function(){'use strict';const root=document.querySelector('[data-msh-progress]'),S=window.MSHStorage;if(!root||!S)return;function e(v){return String(v==null?'':v).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;')}function label(v){const map={practice_completed:'Practice completed',engagement:'Engaged',reflection:'Reflected',learning:'Learned',adapted:'Adapted',preserved:'Preserved',project_completed:'Project completed',movement:'Movement'};return map[v]||String(v||'Progress').replace(/_/g,' ')}function render(){const st=S.getState(),project=S.getActiveProject(st),events=[...st.progressEvents].sort((a,b)=>new Date(b.createdAt||0)-new Date(a.createdAt||0)),attempts=[...st.practiceAttempts].sort((a,b)=>new Date(b.createdAt||0)-new Date(a.createdAt||0)),learning=S.getCurrentLearning(st),reflections=[...st.reflections].sort((a,b)=>new Date(b.createdAt||0)-new Date(a.createdAt||0));const completed=attempts.filter(x=>x.outcome==='did_it').length,adapted=attempts.filter(x=>x.outcome==='changed').length,skipped=attempts.filter(x=>x.outcome==='skipped').length,engaged=attempts.length;root.innerHTML=`${project?`<section class="msh-card msh-card-wide"><p class="msh-card-kicker">Current journey</p><h2>${e(project.title)}</h2><div class="msh-project-path"><article><span>Point A</span><p>${e(project.pointA)}</p></article><b aria-hidden="true">→</b><article><span>Point B</span><p>${e(project.pointB)}</p></article></div></section>`:''}<section class="msh-dashboard-grid"><article class="msh-card"><p class="msh-card-kicker">Experience</p><h2>${engaged} times noticed</h2><p>${completed} completed · ${adapted} changed · ${skipped} skipped. These are observations, not a score.</p></article><article class="msh-card"><p class="msh-card-kicker">Personal knowledge</p><h2>${learning.length} current learning${learning.length===1?'':'s'}</h2><p>${learning[0]?e(learning[0].statement):'Learning will appear as experience becomes useful personal knowledge.'}</p></article><article class="msh-card msh-card-wide"><p class="msh-card-kicker">Your progress story</p><h2>What has been happening</h2>${events.length?`<ol class="msh-progress-timeline">${events.slice(0,12).map(x=>`<li><time>${new Date(x.createdAt).toLocaleDateString(undefined,{month:'short',day:'numeric'})}</time><div><strong>${e(label(x.progressType))}</strong><p>${e(x.statement)}</p></div></li>`).join('')}</ol>`:`<div class="msh-empty-state"><strong>No progress events yet.</strong><p>Progress will begin collecting as you try, notice, reflect, learn, adapt, preserve, and complete things that matter to you.</p></div>`}</article><article class="msh-card"><p class="msh-card-kicker">Reflection</p><h2>${reflections.length} reflection${reflections.length===1?'':'s'}</h2><p>${reflections[0]?e(reflections[0].statement):'Reflections help explain the story behind the activity.'}</p></article><article class="msh-card"><p class="msh-card-kicker">What counts here</p><h2>More than completion</h2><p>Progress can mean moving forward, returning after interruption, learning something useful, changing an unrealistic plan, or protecting what already works.</p></article></section>`}render()})();