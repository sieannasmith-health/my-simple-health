/* My Simple Health — Learning: experience becomes personal knowledge */
(function(){'use strict';const m=document.querySelector('[data-msh-learning]'),s=window.MSHStorage;if(!m||!s)return;let composing=false;function e(v){return String(v==null?'':v).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;')}function id(p){return p+'_'+(crypto.randomUUID?crypto.randomUUID():Date.now()+'_'+Math.random().toString(36).slice(2,7))}function data(){const st=s.getState();return{st,project:s.getActiveProject(st),practice:s.getActivePractice(st),reflections:[...st.reflections].sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt)),learning:[...st.learningEntries].sort((a,b)=>new Date(b.updatedAt||b.createdAt)-new Date(a.updatedAt||a.createdAt))}}function render(){const d=data();m.innerHTML=`<section class="msh-learning-header"><p class="msh-eyebrow">Personal knowledge</p><h1>My Learning</h1><p>Reflection captures what happened. Learning keeps what becomes useful enough to carry forward.</p></section><section class="msh-learning-principle"><strong>You are not collecting rules about yourself.</strong><p>Some things are true only in a certain season, environment, or situation. Learning can stay tentative, change, or be retired when it no longer fits.</p></section>${composing?form(d):home(d)}`}
function home(d){const current=d.learning.filter(x=>x.currentStatus==='current'),tentative=current.filter(x=>x.confidence==='noticing'||x.confidence==='testing'),confirmed=current.filter(x=>x.confidence==='confirmed');return `<section class="msh-learning-actions"><div><p class="msh-card-kicker">From experience</p><h2>What seems worth remembering?</h2><p>Turn a reflection, pattern, or realization into something you want My Simple Health to carry forward.</p></div><button class="msh-button" data-action="compose">Add Learning</button></section>${d.reflections.length?`<section class="msh-learning-source"><p class="msh-card-kicker">Recent reflection</p><blockquote>${e(d.reflections[0].statement)}</blockquote><button class="msh-text-button" data-action="compose">What did I learn from this? →</button></section>`:''}<section class="msh-learning-columns"><div><p class="msh-card-kicker">Still learning</p><h2>Noticing & testing</h2>${tentative.length?tentative.map(card).join(''):`<p class="msh-learning-empty">Nothing here yet. Uncertainty is allowed to stay uncertainty.</p>`}</div><div><p class="msh-card-kicker">Useful right now</p><h2>What I've learned</h2>${confirmed.length?confirmed.map(card).join(''):`<p class="msh-learning-empty">As patterns become clearer, the things you want to remember can live here.</p>`}</div></section>${d.learning.filter(x=>x.currentStatus!=='current').length?`<details class="msh-learning-history"><summary>Past learning</summary>${d.learning.filter(x=>x.currentStatus!=='current').map(card).join('')}</details>`:''}`}
function card(x){return `<article class="msh-learning-card"><span>${x.confidence==='confirmed'?'Confirmed by me':x.confidence==='testing'?'Testing this':'I am noticing'}</span><p>${e(x.statement)}</p>${x.context?`<small>${e(x.context)}</small>`:''}<div><button data-status="${x.id}:confirmed">This feels established</button><button data-status="${x.id}:retired">No longer fits</button></div></article>`}
function form(d){const r=d.reflections[0];return `<form class="msh-learning-form" data-form><p class="msh-card-kicker">Make meaning from experience</p><h2>What do you want to carry forward?</h2>${r?`<div class="msh-learning-reflection"><span>Recent reflection</span><p>${e(r.statement)}</p></div>`:''}<label><span>Learning</span><strong>What seems true or useful to remember?</strong><textarea name="statement" rows="5" required placeholder="For example: I have more energy for this in the morning than after work."></textarea></label><fieldset><legend>How certain does this feel?</legend><div class="msh-learning-confidence"><label><input type="radio" name="confidence" value="noticing" checked><span><strong>I'm noticing this</strong><small>It may be a pattern, but I need more experience.</small></span></label><label><input type="radio" name="confidence" value="testing"><span><strong>I'm testing this</strong><small>I want to intentionally see whether it holds up.</small></span></label><label><input type="radio" name="confidence" value="confirmed"><span><strong>This feels established</strong><small>I have enough experience to treat this as useful knowledge for now.</small></span></label></div></fieldset><label><span>Context</span><strong>When does this seem to be true?</strong><textarea name="context" rows="3" placeholder="Optional: situations, seasons, conditions, exceptions..."></textarea></label><div class="msh-card-actions"><button class="msh-button" type="submit">Save Learning</button><button class="msh-button-secondary" type="button" data-action="cancel">Back</button></div></form>`}
m.addEventListener('click',ev=>{const a=ev.target.closest('[data-action]');if(a){composing=a.dataset.action==='compose';render();return}const b=ev.target.closest('[data-status]');if(!b)return;const [entryId,status]=b.dataset.status.split(':');s.updateState(st=>{const x=st.learningEntries.find(y=>y.id===entryId);if(!x)return st;if(status==='confirmed'){x.confidence='confirmed';x.currentStatus='current'}else{x.currentStatus='retired'}x.updatedAt=new Date().toISOString();st.progressEvents.push({id:id('progress'),projectId:x.projectId||null,progressType:'learning_updated',statement:status==='confirmed'?`Confirmed learning: ${x.statement}`:`Updated learning that no longer fits: ${x.statement}`,createdAt:new Date().toISOString()});return st});render()});m.addEventListener('submit',ev=>{if(!ev.target.matches('[data-form]'))return;ev.preventDefault();const fd=new FormData(ev.target),d=data(),statement=fd.get('statement').trim();s.updateState(st=>{st.learningEntries.push({id:id('learning'),projectId:d.project?d.project.id:null,practiceId:d.practice?d.practice.id:null,sourceReflectionId:d.reflections[0]?d.reflections[0].id:null,statement,context:fd.get('context').trim(),confidence:fd.get('confidence'),currentStatus:'current',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()});st.progressEvents.push({id:id('progress'),projectId:d.project?d.project.id:null,progressType:'learning',statement:`Learned: ${statement}`,createdAt:new Date().toISOString()});return st});composing=false;render()});render()})();